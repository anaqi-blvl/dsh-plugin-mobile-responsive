/**
 * Node half of dsh-mobile-responsive. Everything this plugin does happens in
 * the browser (CSS injection); the host half is a no-op cordis plugin whose
 * only job is to be a loadable entry so the client half joins the web roster.
 */
export default function () {
  /* intentionally empty */
}
